/**
 * The area's own page.
 *
 * Everything an operator actually works with arrives through the slots beside this one, so this
 * carries only what is true whatever rank you hold. The shell renders it above them.
 */
export default function AdminPage() {
  return null;
}
