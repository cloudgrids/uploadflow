import { AccountsPanel } from '../../../../components/site/operator/AccountsPanel';

/** Support and up. The rank is enforced by the API; the shell decides whether to render this. */
export default function PeopleSlot() {
  return <AccountsPanel />;
}
