import { CampaignsPanel } from '../../../../components/site/operator/CampaignsPanel';

/** Admin and up. The rank is enforced by the API; the shell decides whether to render this. */
export default function CampaignsSlot() {
  return <CampaignsPanel />;
}
